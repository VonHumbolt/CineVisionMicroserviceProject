server:
  port: 8080

spring:
  application:
    name: movieService
  datasource:
    username: 'postgres'
    url: jdbc:postgresql://localhost:5432/cine_vision
    password: '12345'
    jpa:
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: 'true'
      hibernate:
        ddl-auto: update
      show-sql: 'true'