server:
  port: 8083
spring:
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: email
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring:
          json:
            trusted:
              packages: "*"
    topic:
      name: email_topics

  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    port: 587
    username: python3.testmail@gmail.com
    password: obndjvibcsqezjuq
    protocol: smtp
    test-connection: false
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  application:
    name: emailService
